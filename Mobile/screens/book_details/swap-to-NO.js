import { Text, View, Alert, Image, ScrollView, TouchableHighlight } from "react-native";

// requires parent to pass swapId as object key

export default function swapToNO (data) {
    
    // console.log(`Swap ID: ${data.swapId}`);

    // CAPSTONE set swapDB item availability to NO, if fail, failed transaction, return false
    // if (fail) {
    //     Alert.alert(
    //         'Error',
    //         `Removal of book failed, someone else was faster..`,
    //         [
    //         { text: "OK" }
    //         ]
    //     );
    //     return false;
    // };

    // CAPSTONE if swapID item availability set successfully to NO, return true;
    return true;
};
